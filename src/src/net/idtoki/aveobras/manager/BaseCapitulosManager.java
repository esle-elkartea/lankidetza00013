package net.idtoki.aveobras.manager;

import java.util.logging.Logger;
import net.idtoki.aveobras.model.CapitulosPeer;
import net.idtoki.aveobras.group.CapitulosGroupBean;
import net.idtoki.aveobras.model.Capitulos;
import net.zylk.torque.SearchQuery;
import net.zylk.torque.BaseQuery;
import net.zylk.torque.TorqueUtils;
import net.zylk.torque.manager.BaseManager;
import org.apache.torque.TorqueException;
import java.util.Enumeration;



import org.apache.torque.util.Criteria;
import org.apache.torque.util.UniqueList;
import net.zylk.tools.ajax.AjaxUtils.DinamicGridBean;

/**
 * This class was autogenerated by Torque on:
 *
 * [Wed Oct 25 17:01:04 CEST 2006]
 *
 */
public abstract class BaseCapitulosManager extends BaseManager
{
	protected static final Logger logger = Logger.getLogger(
	   "net.idtoki.aveobras.managers.BaseCapitulosManager");

	public static final Criteria buildSearchCriteria(DinamicGridBean dgb)
	{
		return BaseManager.buildSearchCriteria(dgb, new CapitulosManager());
	}

	public static final Criteria buildSearchCriteria(String query, int offset, int limit)
	{
		return BaseManager.buildSearchCriteria(query, offset, limit, new CapitulosManager());
	}

	public static final Criteria buildSearchCriteria(String query)
	{
		return BaseManager.buildSearchCriteria(query, new CapitulosManager());
	}

	protected void addOrSearchCriteria(Criteria criteria,String campo,String valor, String condicion){
		Criteria.Criterion criterion = CapitulosManager.getSearchCriterion(campo,valor, condicion);
		//Si existe ya un criterion hay que utilizarlo para añadir el or sino utilizará un and
		Enumeration enumeration =criteria.elements();
		if(enumeration.hasMoreElements()){
			Criteria.Criterion cr = (Criteria.Criterion) enumeration.nextElement();
			cr.or(criterion);
		}else{
			criteria.or(criterion);
		}
	}

	protected void addAndSearchCriteria(Criteria criteria,String campo, String valor, String condicion){
		Criteria.Criterion criterion = CapitulosManager.getSearchCriterion(campo,valor, condicion);
		criteria.and(criterion);
	}

	protected static Criteria.Criterion getSearchCriterion(String campo,String valor, String condicion){
        Criteria criteria =  new Criteria();
        Criteria.Criterion criterion = null;

			if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(CapitulosPeer.IDCAPITULO)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,CapitulosPeer.IDCAPITULO,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,CapitulosPeer.IDCAPITULO,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,CapitulosPeer.IDCAPITULO,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,CapitulosPeer.IDCAPITULO,valor,true);
		}
			if(campo.equals("all") || campo.equals(BaseManager.campoToLowerCase(CapitulosPeer.NOMBRE)))
		{
			if(condicion.equals(BaseQuery.SingleQuery.EQUAL_CONDITION))
				criterion = TorqueUtils.addOrEqualCriteria(criterion,CapitulosPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LIKE_CONDITION))
				criterion = TorqueUtils.addOrLikeCriteria(criterion,CapitulosPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.LESS_CONDITION))
				criterion = TorqueUtils.addOrLessCriteria(criterion,CapitulosPeer.NOMBRE,valor,true);
			else if (condicion.equals(BaseQuery.SingleQuery.GREATER_CONDITION))
				criterion = TorqueUtils.addOrGreaterCriteria(criterion,CapitulosPeer.NOMBRE,valor,true);
		}
		
		return criterion;
	}

	public static final CapitulosGroupBean getCapituloss() {
    	return BaseCapitulosManager.getCapituloss(false);
	}

	public static final CapitulosGroupBean getCapituloss(boolean paginado) {
    	return BaseCapitulosManager.getCapituloss(new Criteria(), paginado,0);
	}

	public static final CapitulosGroupBean getCapituloss(Criteria c) {
		return BaseCapitulosManager.getCapituloss(c,false,0);
	}

	public static final CapitulosGroupBean getCapituloss(Criteria criteria,boolean paginado, int pagina) {
		CapitulosGroupBean objCapitulosGroupBean = new CapitulosGroupBean();
		objCapitulosGroupBean.setPaginaActual(pagina);
		objCapitulosGroupBean.setCapituloss(criteria, paginado);
		return objCapitulosGroupBean;
	}

							
	public static final Capitulos getCapitulos(int id_Idcapitulo) {
		Criteria c = new Criteria();
			c.add(CapitulosPeer.IDCAPITULO, id_Idcapitulo);
			return BaseCapitulosManager.CapitulossGroupBean2Capitulos(BaseCapitulosManager.getCapituloss(c));
	}

	public static final Capitulos CapitulossGroupBean2Capitulos(CapitulosGroupBean obj){
		if(obj.size() == 1)
			return obj.getCapitulos(0);
		else
			return null;
	}

	private static void updateCapitulosAttribute(int id_Idcapitulo, String valor, String campo) throws Exception
	{
		Capitulos obj = BaseCapitulosManager.getCapitulos(id_Idcapitulo);
			if(campo.equals(CapitulosPeer.IDCAPITULO))
					obj.setIdcapitulo(Integer.parseInt(valor)); //int
					if(campo.equals(CapitulosPeer.NOMBRE))
					obj.setNombre(valor); //String
					obj.save();
	}

	public static void updateIdcapitulo(int id_Idcapitulo, String valor) throws Exception
	{
		updateCapitulosAttribute(id_Idcapitulo, valor, CapitulosPeer.IDCAPITULO);
	}
	public static void updateNombre(int id_Idcapitulo, String valor) throws Exception
	{
		updateCapitulosAttribute(id_Idcapitulo, valor, CapitulosPeer.NOMBRE);
	}

			public void addJoins(Criteria criteria)
	{
	 //no tiene claves externas asi que el metodo no hace nada
	}
	

  public static void deleteCapitulos(int id_Idcapitulo) throws TorqueException
  {
  	CapitulosPeer.doDelete(CapitulosManager.getCapitulos(id_Idcapitulo));
  }

  public static Capitulos addUpdateCapitulos(Capitulos obj) throws TorqueException
  {
	CapitulosGroupBean gb = new CapitulosGroupBean();
	gb.setElemento(obj);
	gb.save();
	return obj;
  }

}

